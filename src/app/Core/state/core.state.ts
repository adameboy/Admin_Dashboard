import { AppState } from "src/app/Core/state/app/app.state";

export interface State {
    app: AppState,
    // auth: AuthState,
    // users: UserState,
    // dashboard: DashBoardState,
    // invoices: InvoiceState,
    // catalogs: CatalogsState,
    // bankResume: BankResumeState,
}
